script(src='bower_components/angular/angular.min.js')

script(src='bower_components/angular-route/angular-route.min.js')
script(src='bower_components/angular-animate/angular-animate.min.js')

script(src='bower_components/angular-aria/angular-aria.min.js')

script(src='bower_components/angular-material/angular-material.min.js')

script(src='bower_components/angular-messages/angular-messages.min.js')

script(src='bower_components/ngprogress/build/ngprogress.min.js')

script(src='bower_components/angular-local-storage/dist/angular-local-storage.min.js')

script(src='app-angular.js')

script(src='scripts/services/utilService.js')
script(src='scripts/services/ngProgressbarService.js')
script(src='scripts/directives/wuHeader.js')
script(src='scripts/directives/wuPagination.js')
script(src='scripts/controllers/homeController.js')
script(src='scripts/controllers/recipeController.js')
script(src='scripts/controllers/recipeDetailsController.js')


script.
	// This is called with the results from from FB.getLoginStatus().
	function statusChangeCallback(response) {
	console.log('statusChangeCallback');
	console.log(response);
	// The response object is returned with a status field that lets the
	// app know the current login status of the person.
	// Full docs on the response object can be found in the documentation
	// for FB.getLoginStatus().
	if (response.status === 'connected') {
	  // Logged into your app and Facebook.
	  testAPI();
	} else if (response.status === 'not_authorized') {
	  // The person is logged into Facebook, but not your app.

	  //document.getElementById('usrname').innerHTML = '';
	  notestAPI();  
	} else {
	  // The person is not logged into Facebook, so we're not sure if
	  // they are logged into this app or not.
	  //document.getElementById('status').innerHTML = 'Please log ' +
	  //  'into Facebook.';
	  //document.getElementById('usrname').innerHTML = '';  
	   notestAPI();
	}
	}

	function checkLoginState() {
	FB.getLoginStatus(function(response) {
	  statusChangeCallback(response);
	 
	});
	//testAPI();
	}

	window.fbAsyncInit = function() {
	FB.init({
	appId      : '1859751924312499',
	cookie     : true,  // enable cookies to allow the server to access 
	                    // the session
	xfbml      : true,  // parse social plugins on this page
	version    : 'v2.8' // use graph api version 2.8
	});

	};

	// Load the SDK asynchronously
	(function(d, s, id) {
	var js, fjs = d.getElementsByTagName(s)[0];
	if (d.getElementById(id)) return;
	js = d.createElement(s); js.id = id;
	js.src = "//connect.facebook.net/en_US/sdk.js";
	fjs.parentNode.insertBefore(js, fjs);
	}(document, 'script', 'facebook-jssdk'));

	// Here we run a very simple test of the Graph API after login is
	// successful.  See statusChangeCallback() for when this call is made.
	function testAPI() {
	console.log('Welcome!  Fetching your information.... ');
	FB.api('/me', function(response) {
	  console.log('Successful login for: ' + response.name);
	  document.getElementById('usrname').innerHTML = response.name + '!';
	});
	}

	function notestAPI() {
	FB.api('/me', function(response) {
	  console.log('Successful logout for: ' + response.name);
	  document.getElementById('usrname').innerHTML = '';
	});
	}

script.
	(function(d, s, id) {
	var js, fjs = d.getElementsByTagName(s)[0];
	if (d.getElementById(id)) return;
	js = d.createElement(s); js.id = id;
	js.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1";
	fjs.parentNode.insertBefore(js, fjs);
	}(document, 'script', 'facebook-jssdk'));	